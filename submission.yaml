# Generated by `programbench submit package` from: docs/public_benchmark/programbench_2solve_cleanroom_v6/20260702_dtl-supermath-2solve-cleanroom-v6
# [auto] fields are recomputed on every `programbench submit package`; all other fields are preserved.
schema_version: 1

submission_id: "20260702_dtl-supermath-2solve-cleanroom-v6"
submitter:
  name: "Kyal"
  contact: "@kyal102"
  affiliation: "JARVI3 / EcoKure"

system:
  agent: "DTL/SuperMath verifier + mini-SWE-agent"
  description_url: "README.md"
  is_os_model: false
  is_os_scaffold: true
  model: "Jarvi3: Themis-G"
  provider: "JARVI3"
  type: "agent"

eval:
  programbench_version: "1.2.3"
# Scores are not stored here: the leaderboard recomputes them from _stats/score.json with the
# registry's current ignored-tests list, so any cached numbers would just go stale.

